{
  "name": "myblog-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @myblog/api & npm run dev --workspace @myblog/web",
    "dev:web": "npm run dev --workspace @myblog/web",
    "dev:api": "npm run dev --workspace @myblog/api",
    "build": "npm run build --workspace @myblog/web",
    "typecheck": "npm run typecheck --workspace @myblog/api && npm run typecheck --workspace @myblog/web",
    "lint": "npm run lint --workspace @myblog/web"
  }
}
